STScI Reveal.js Talk — placeholder notes

File:
- stsci_reveal_talk.html

How to replace placeholders:
1. Open stsci_reveal_talk.html in a text editor.
2. Search for: PLACEHOLDER:
3. For an image placeholder, replace the whole placeholder <div> with:
   <img src="your_image.png" class="fit-media">
4. For the movie placeholder, replace the whole placeholder <div> with:
   <video controls src="your_movie.mp4" class="fit-media"></video>

Notes:
- The title slide currently uses a placeholder title: "Your Talk Title Here".
- The deck uses Reveal.js from a CDN, so opening it normally in a browser requires internet access.
